include ./../../ui-kit/ui-kit
<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title landing page
body
  +header({signed:false})
  section.sign-in-page
    .sign-in-page__wrapper
      +registration()
    .sign-in-page__wrapper
      +sign-in()
  +footer()
