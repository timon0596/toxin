mixin textfield({placeholder,material_icons,modifier})
	.text-field(class={'subscription-text-field': modifier==='subscription','masked-text-field js-masked-text-field': modifier==='masked'})
		input(placeholder= placeholder)
		if material_icons
			i.material-icons= material_icons